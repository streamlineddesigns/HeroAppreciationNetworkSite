{% extends "app.twig" %}

{% block pageStyles %}
    <link rel="stylesheet" href="https://unpkg.com/bootstrap-table@1.18.3/dist/bootstrap-table.min.css">
    
{% endblock %}

{% block title %}Icons{% endblock %}

{% block content %}

  {% if hasMessages() %}
    {% if successMessage() %}
        <script>swal("Success!", "{{successMessage()}}", "success");</script>
    {% elseif errorMessage() %}
        <script>swal("Error!", "{{errorMessage()}}", "error");</script>
    {% endif %}
  {% endif %}

  <div class="container my-5">

        <a href="{{url('/superuser/icons/create')}}" class="btn btn-success mb-5 float-right"><i class="bi bi-plus"></i> New</a>

        {% if icons %}
            <table class="table table-striped table-dark table-bordered table-responsive" data-toggle="table" data-pagination="true" data-search="true">
                <thead>
                    <tr>
                        <th class="mobile-hidden" data-sortable="true">ID</th>
                        <th data-sortable="true">Name</th>
                        <th class="mobile-hidden text-center" data-sortable="true">Class</th>
                        <th class="">Icon</th>
                        <th class="">Icon Type</th>
                        <th class="actions"></th>
                    </tr>
                </thead>
                <tbody>
                {% for icon in icons %}

                    <tr>
                        <td class="mobile-hidden">
                            {{ icon.id }}
                        </td>
                        <td class="primary-mobile-td">
                            {{ icon.name }}
                        </td>
                        <td class="mobile-hidden text-center">
                            {{ icon.class_name }}
                        </td>
                        <td class="mobile-hidden text-center">
                            <i class="{{ icon.class_name }}"></i>
                        </td>
                        <td class="mobile-hidden text-center">
                            {{ icon.type }}
                        </td>
                        <td class="mobile-td text-center">
                            <a href="/superuser/icons/{{ icon.id }}/edit" class="btn btn-primary"><i class="bi bi-pencil"></i><span class="mobile-hidden"> Edit</span></a>
                            <a href="/superuser/icons/{{ icon.id }}" class="btn btn-secondary"><i class="bi bi-eye"></i><span class="mobile-hidden"> View</span></a>
                            <a href="javascript:FunApp.deleteResource({{ icon.id }}, 'icon', 'delete-form-{{ icon.id }}')" class="btn btn-danger"><i class="bi bi-trash"></i><span class="mobile-hidden"> Delete</span></a>
                            <form method="post" action="/superuser/icons/{{ icon.id }}/delete" id="delete-form-{{ icon.id }}"></form>
                        </td>
                    </tr>

                {% endfor %}
                </tbody>
            </table>
        {% else %}
            No games found.
        {% endif %}
    </div>

{% endblock %}

{% block pageScripts %}
    <script src="https://unpkg.com/bootstrap-table@1.18.3/dist/bootstrap-table.min.js"></script>
    <script src="{{url('/assets/js/FunApp.js')}}"></script>
{% endblock %}